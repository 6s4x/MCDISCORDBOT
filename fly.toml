app = "mc-fly-bot"

[env]
  NODE_ENV = "production"

[processes]
  app = "node index.js"

[[services]]
  processes = ["app"]
  protocol = "tcp"
  internal_port = 8080

  [[services.ports]]
    port = 8080
